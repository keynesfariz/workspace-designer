/** Pixel width of one space unit column */
export const UNIT_WIDTH = 140;
/** Pixel height of the wall zone (above the floor line) */
export const WALL_HEIGHT = 180;
/** Pixel height of the floor zone (includes table body + table-top slots + table-floor slots) */
export const FLOOR_HEIGHT = 240;
/** Total canvas height */
export const CANVAS_HEIGHT = WALL_HEIGHT + FLOOR_HEIGHT;
/** Height of the table-TOP slot row (items on the table surface) */
export const TABLE_TOP_H = 44;
/** Height of individual icons rendered inside table-top slots */
export const TABLE_TOP_SLOT_H = 40;
/** Height of the table-FLOOR slot row (chairs in front of the table) */
export const TABLE_FLOOR_H = 52;
/** Height of individual icons rendered inside table-floor slots */
export const TABLE_FLOOR_SLOT_H = 48;
